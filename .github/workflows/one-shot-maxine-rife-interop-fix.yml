name: One-shot Maxine RIFE interop fix

on:
  push:
    branches:
      - feature/rife-tensorrt-interpolation

jobs:
  probe:
    runs-on: windows-2022
    steps:
      - run: Write-Host 'one-shot probe ran'
        shell: pwsh
