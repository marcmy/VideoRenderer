MPCVR SVP4 grid24 vector-source TEST

Double-click Install-Test.cmd with MPC-HC closed. The script requests elevation,
backs up the currently installed K-Lite MPCVR binaries on first use, installs
both x86 and x64 test payloads, and verifies their SHA-256 hashes after copying.

Restore-Previous.cmd restores that first pre-test backup.

This is a research TEST artifact. It is not Build 4.
